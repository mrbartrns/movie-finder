[build]
    command = "yarn build"
    functions = "functions"
    publish = "dist"

[dev]
    framework = "#custom"
    command = "yarn start"
    targetPort = 3000
    port = 5000
    publish = "dist"
    autoLaunch = false